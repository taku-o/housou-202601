#include <stdio.h>

int main(void)
{
	int koku = 75; /* int型変数kokuを 75 で初期化 */
	int san  = 93; /* int型変数san を 93 で初期化 */
	int sya  = 68; /* int型変数sya を 68 で初期化 */

	printf("国語は%d点、算数は%d点、社会は%d点です。\n", koku, san, sya);

	return 0;
}
